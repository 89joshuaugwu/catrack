import AppShell from "@/components/shells/AppShell";
import ScoreHistoryView from "@/components/organisms/ScoreHistoryView";
import { demoUser, mockAttempts, mockCourses, mockQuizzes } from "@/lib/mock-data";
export default function ScoresPage(){const user=demoUser.student;const attempts=mockAttempts.filter(a=>a.uid===user.uid);return <AppShell role="student" userName={user.displayName}><div className="mb-8"><p className="eyebrow">Student workspace</p><h1 className="mt-1 font-display text-3xl font-bold tracking-tight">CA progress</h1><p className="mt-2 text-text-secondary">See how each graded quiz contributes to your continuous assessment total.</p></div><div className="grid gap-5">{mockCourses.map(course=>{const quizzes=mockQuizzes.filter(q=>q.courseId===course.id);return <ScoreHistoryView key={course.id} studentName={user.displayName} course={course} quizzes={quizzes} attempts={attempts.filter(a=>quizzes.some(q=>q.id===a.quizId))}/>;})}</div></AppShell>;}
